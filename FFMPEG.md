# FFmpeg

This project uses a local portable FFmpeg binary installed with `ffmpeg-static`.

Run FFmpeg from the project root:

```cmd
.\ffmpeg.cmd -version
```

The binary lives under `.tools/`, which is ignored by Git.
